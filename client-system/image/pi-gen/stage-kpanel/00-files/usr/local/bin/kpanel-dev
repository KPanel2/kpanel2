#!/usr/bin/env bash
set -euo pipefail

if [[ ${EUID:-$(id -u)} -eq 0 ]]; then
  exec /usr/local/bin/kpanel-set-mode dev
fi

if command -v sudo >/dev/null 2>&1; then
  exec sudo /usr/local/bin/kpanel-set-mode dev
fi

echo "kpanel-dev requires root privileges. Run: sudo kpanel-set-mode dev"
exit 1
